// import jwt from 'jsonwebtokens';

// function verify(token){ 
//           const jwtToken = token.split(' ')[1];
//           const identity = jwt.verify(jwtToken,process.env.JWT_KEY);
//         if(identity.user)
          
            
// }